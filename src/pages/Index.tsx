/**
 * Index Page — The main entry point for the "/" route.
 * Renders the full app shell via AppLayout.
 */

import AppLayout from "@/components/layout/AppLayout";

const Index = () => {
  return <AppLayout />;
};

export default Index;
